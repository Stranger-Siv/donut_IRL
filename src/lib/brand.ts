/** Product / site name (nav, tab title, domain). */
export const SITE_NAME = "Donut IRL";

/** In-game world this marketplace is for (use sparingly in UI; spell out in metadata + key spots). */
export const COMMUNITY_NAME = "Donut SMP";

/** Default HTML title: search and tabs — one clear IRL + SMP line. */
export const SITE_TITLE_DEFAULT = `${SITE_NAME} — real money for ${COMMUNITY_NAME} in-game money`;

/**
 * Meta description: main place the “IRL + Donut SMP” story appears for link previews and SEO.
 */
export const SITE_DESCRIPTION = `IRL (cash) marketplace for ${COMMUNITY_NAME} in-game money — sell for INR, live rates and order tracking. Independent service, not the server.`;

export const SITE_OG_DESCRIPTION = SITE_DESCRIPTION;
